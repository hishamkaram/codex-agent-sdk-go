pre-commit:
  parallel: true
  commands:
    go-vet:
      glob: "*.go"
      run: go vet ./...
    golangci-lint:
      glob: "*.go"
      run: env GOWORK=off golangci-lint run --timeout=5m ./...
    govulncheck:
      glob: "*.go"
      run: govulncheck ./...
    go-fmt:
      glob: "*.go"
      run: |
        UNFMT=$(gofmt -l .)
        if [ -n "$UNFMT" ]; then
          echo "Files not formatted:"
          echo "$UNFMT"
          exit 1
        fi
